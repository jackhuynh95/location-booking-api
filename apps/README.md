# Apps

This directory contains application roots.

Current planned apps:

- `server`: NestJS REST API for location and booking management.
- `admin`: optional administrative UI or internal console, pending user approval.

Each app should stay self-contained once implementation begins. Shared decisions should be documented in `docs/` before adding shared packages or workspace tooling.
